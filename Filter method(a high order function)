//FILTER takes as input such a function, which takes int and returns boolean 
//FILTER method is basically used to filter out things 

fun main() {
    val list=listOf<Int>(1, 2, 5, 8, 7)
    list.filter({
      it % 2==0
    }).forEach({
       println("$it")
    })
}

//OUTPUT=2
         8
