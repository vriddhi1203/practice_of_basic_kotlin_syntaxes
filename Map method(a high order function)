//MAP is a high order function which basically transforms the list by taking input as 'Int' data type and returns 'Any' data type

fun main() {
    val list=listOf(1,2,5,8,7)
    list.filter({
      it % 2==0
    }).map({
      it*it
    }).forEach({
       println("$it")
    })
}

//OUTPUT=4
         64
